[package]
name = "bota-device-sdk-core"
version = "0.1.0-alpha.1"
edition = "2024"
license = "MIT"
publish = false

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }

[lib]
path = "src/lib.rs"
