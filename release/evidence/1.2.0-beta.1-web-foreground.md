# Web Foreground Release Evidence: 1.2.0-beta.1

## Status

**BLOCKED on supervised physical-device acceptance.**

The browser-feasible foreground implementation and automated package/Chromium
gates are complete. No physical browser/device session was selected for this
documentation task, so every hardware row remains `NOT RUN`. This file does not
authorize a tag, publication, protected-environment approval, or release.

## Candidate identity

- Package: `@bota.dev/web-sdk@1.2.0-beta.1`
- Foreground implementation revision:
  `3b98be6c988bb73917c073cc2c355dfabec09547`
- Package manager: `npm@12.0.2`
- Packed archive: `bota.dev-web-sdk-1.2.0-beta.1.tgz`
- Packed archive size: 354,395 bytes
- Packed archive SHA-256:
  `d527fb80c117fdeabf884fc3383ef25b079741180cb13b50e4eeb4c627bac735`
- Normalized content SHA-256:
  `618e2718215a6f2ff37db60c6e9794a1c01be5e45b9b80c82d7dfdc7adc5b8dc`
- Package inventory entries: 52
- Package inventory checksum:
  `5b78b385c373e1407295fdf6cb9bde424085d91c7d1cc25b981285608de52532`

These values come from two byte-identical clean `npm run web:verify` runs at
the implementation revision above. Task 14 changes public documentation,
including the README copied into the npm package; Task 15 must regenerate the
candidate from its final clean release revision and update this identity before
publication. The evidence file cannot self-certify the commit that contains
it.

## Automated verification

The complete automated gate was run twice with the release directory removed
between runs:

```bash
rm -rf target/web-release
PLAYWRIGHT_BROWSERS_PATH="$PWD/target/playwright-browsers" npm run web:verify
rm -rf target/web-release
PLAYWRIGHT_BROWSERS_PATH="$PWD/target/playwright-browsers" npm run web:verify
```

Both runs reported:

- 365 Web tests passed, 0 failed;
- Web type-check and production package build passed;
- 26 release-tooling tests passed, 0 failed;
- the exact tarball installed into a clean Vite production consumer;
- 7 Playwright Chromium cases passed, 0 failed;
- raw tarball, normalized content, per-file inventory, and inventory checksum
  matched across runs.

The automated browser uses deterministic fake Bluetooth and an OPFS-compatible
fake with real IndexedDB. Provider calls, uploads, and device behavior are
fakes. These results are not physical-device evidence.

## Implemented foreground surface

- explicit picker connection, disconnect, exact serial verification, and fresh
  snapshot;
- exact previously authorized device reconnect when browser enumeration is
  available;
- recording list, legacy synchronization, encrypted-upload-v2 synchronization
  when freshly advertised, cancellation, recovery, and receipt-gated confirm;
- provisioning, remove-only deprovisioning, and connection settings;
- WiFi scan, configure, disconnect, status, and status subscription;
- host-authorized recording start and stop;
- integrity-checked firmware download, transfer, progress, cancellation,
  reload recovery, reboot, and exact-device reconnect;
- sanitized single-owner device-log subscription and explicit removal;
- tenant-scoped IndexedDB/OPFS persistence and BLE-free logout cleanup.

## Known exclusions

- background scan, service-worker Bluetooth, and closed-tab work;
- live recording streaming;
- authenticated destructive factory reset;
- Safari/iOS fallback transport or Bluetooth polyfill;
- Flutter Web and Windows;
- Bota API authentication or a generated backend API client.

## Supervised physical matrix

Follow
[`docs/testing/web-physical-device.md`](../../docs/testing/web-physical-device.md).
No row below has been executed for this candidate.

| Case | Result | Evidence |
|---|---|---|
| Picker and exact identity | `NOT RUN` | None |
| Authorized exact-device reconnect | `NOT RUN` | None |
| Fresh snapshot | `NOT RUN` | None |
| Legacy recording sync | `NOT RUN` | None |
| Encrypted Upload v2 when advertised | `NOT RUN` | None |
| Provisioning close-loop | `NOT RUN` | None |
| Connection settings read/write/restore | `NOT RUN` | None |
| WiFi scan/configure/status/disconnect/restore | `NOT RUN` | None |
| Recording start/stop | `NOT RUN` | None |
| OTA progress/reboot/exact reconnect/readback | `NOT RUN` | None |
| Device logs and removal | `NOT RUN` | None |
| Recording and OTA cancellation | `NOT RUN` | None |
| Recording and OTA reload recovery | `NOT RUN` | None |
| Remove-only deprovision after authorized operations | `NOT RUN` | None |
| Destroy and tenant cleanup | `NOT RUN` | None |

Run metadata is also `NOT RUN`: date/timezone, reviewer, browser/version,
host OS, device model/serial, firmware before/after, candidate revision, and
test tenant/evidence ID.

## Release decision

Release remains `BLOCKED`. It may advance only after all required physical rows
pass on the exact final candidate, conditional capability dispositions are
reviewed, the package evidence is regenerated from the final clean revision,
and the synchronized non-Web release gates also pass. No remote CI/CD,
registry, tag, publish, or protected-environment action is evidence here.
