# Web Foreground Release Evidence: 1.2.0-beta.3

## Status

Supervised physical-device acceptance: **NOT RUN**. The release owner requested
public beta rollout first so the exact published package can be tested against
a production device. This is not hardware acceptance or general availability.

The `v1.2.0-beta.2` publish job stopped at candidate inventory comparison before
any registry write. Its immutable tag and evidence are retained. Beta.3 fixes
that comparison; its automated release gates must pass for the exact tagged
source before publication. Record the final tag, CI inventory, and public
package digest after those gates complete. No device serial, backend credential,
WiFi secret, recording data, or private log belongs in this file.

## Post-release acceptance

Run every applicable row in
[`docs/testing/web-physical-device.md`](../../docs/testing/web-physical-device.md)
with a supervised, exact-serial physical device and the public
`@bota.dev/web-sdk@1.2.0-beta.3` tarball. Keep all rows `NOT RUN` until actually
observed. Any failure must be triaged before another synchronized release.
