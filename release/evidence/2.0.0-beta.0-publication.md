# App SDK 2.0.0-beta.0 Publication

Published on 2026-09-24 from immutable source
`dd672a5865ba460ca3e97420e97461eb096dfb4f` and tag `v2.0.0-beta.0`.
Main CI `35965596618` passed all five-platform gates. Candidate inventory SHA-256:
`1c4865edbc38584d1ddeab6927687b22370b15a981da3d4b2d8fac3d3ea4d573`.

| Distribution | Exact published identity | Evidence |
| --- | --- | --- |
| Apple SwiftPM/CocoaPods | `BotaAppSDK` | Public consumers; checksummed GitHub archives and exact Trunk/CDN spec |
| Android Maven | `dev.bota:bota-app-sdk` | All 30 signed file hashes; public API 26/35 consumers |
| React Native npm | `@bota.dev/react-native-app-sdk` | Public tarball SHA-256 `7a24a995575a656f6fe24335727afd4e7710333e0df027eb94113e5d60b8e6f9` |
| Web npm | `@bota.dev/web-app-sdk` | Public tarball SHA-256 `90b940bdd8d3b2c57b37e98d0eaec101be922c814436dbdfbeed216335a149ae` |
| Flutter pub.dev | `bota_app_sdk` | All 57 public file hashes match ordered artifact `10818307134` |

Flutter's normalized public archive SHA-256 is
`3b2c1da0b4836e9eada13230b2860a11670123857ee6bd10c8e5f39ec6a7f67e`.
Its raw public archive SHA-256 is
`64d850444ec6447c6b81fd1fb4a605066c33d33f6455c8a675cfdcca002ffa34`.
Fresh Flutter Android/iOS release builds passed before publication. Both
`35971649362`'s public verifier and separate workflow `35971649509` verified
the occupied pub.dev archive; the separate workflow skipped its upload job.

The final job in `35971649362` failed before attaching evidence because `gh`
had neither a checkout nor an explicit repository. Recovery attached the exact
eleven missing evidence assets manually, preserving every existing asset.
Flutter's manifest is named `flutter-release-manifest.json` to avoid replacing
the native manifest. The release is public and marked prerelease. The tagged
workflow failure remains visible; it is not claimed as a fully green run.

New npm `beta` and `latest` tags both point to this first version under the
owner-approved bootstrap exception. Future beta releases must not advance
`latest`. All legacy RN/Web versions and tags remain unchanged, including
maintenance RN `latest` at `0.0.67`.

React Native trusted publishing is configured. On 2026-09-24, Flutter's GitHub
publisher was saved and verified after reload: `bota-dev/app-sdk`, tag pattern
`v{{version}}`, push events only, required environment `release`. Manual
publishing remains enabled; workflow-dispatch and GCP publishing remain
disabled. Ownership is unchanged. The main-branch workflow now passes the
required environment to the reusable upload job as well as protecting its gate;
a red-to-green regression and all 92 release-tooling tests passed locally.
GitHub's existing reviewer and branch/tag policies were read back unchanged.
Web trusted publishing was created on 2026-09-24 using npm 12.0.2's interactive
`trust github` command, then independently read back on the package settings
page: repository `bota-dev/app-sdk`, workflow `release.yml`, environment
`release`, permissions `publish` and `stage publish`. The command returned trust
ID `a3363f7f-5fbb-4dea-bedc-f34847420b80`. Existing package-access settings and
maintainers were unchanged. Both new npm packages still have exactly one
version with `beta` and `latest` at `2.0.0-beta.0`; legacy RN/Web versions and
dist-tags remain unchanged.
No automated OIDC upload has been demonstrated for the new names. Hardware
acceptance is NOT RUN. Demo and Bota One dependencies were not changed.

Artifacts: [GitHub prerelease](https://github.com/bota-dev/app-sdk/releases/tag/v2.0.0-beta.0).

Post-publication [public consumer checks](2.0.0-beta.0-public-consumers.md)
passed for Web, RN, and Flutter iOS. Flutter Android requires an explicit SDK
path in published beta.0; its normal-install lookup is fixed and locally
verified on main, pending a subsequent release. Publication verification is
not an assertion that this consumer issue is absent.
