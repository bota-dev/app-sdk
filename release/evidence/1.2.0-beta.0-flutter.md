# Flutter 1.2.0-beta.0 Release Evidence

## Status

Prepared and verified locally; not published. This file records candidate
evidence only. It is not evidence of a pub.dev publication, Apple pod push,
Maven or npm publication, GitHub tag, or GitHub release.

## Candidate Identity

- Package: `bota_flutter_sdk@1.2.0-beta.0`
- Local source revision:
  `2a65e43ee462b3231546e07274abf3561c1503eb`
- Generator: Pigeon `28.0.0`
- Archive: `bota_flutter_sdk-1.2.0-beta.0.tar.gz`
- Archive size: 154,437 bytes
- Inventory: 57 files
- Raw archive SHA-256:
  `f880d3925dca2566a099f2eed8c15ec547e9fa2a4f4e7a4992d8f29eb74f834f`
- Normalized archive SHA-256:
  `c3f5bd28a122938a26304ae04157620953f3cf1a563b74a5c8617a364cd73c11`
- Package inventory SHA-256:
  `1f9cea16808788960be93ca18b013833ad5ac4aa9a204de1e793a7615c7a96bf`
- Release manifest SHA-256:
  `852b3f7a4d287ae63ba3d25459121655de6357e4ed72a44a197f7c04cbe1470c`
- Apple core archive and SwiftPM checksum:
  `0ffff173028b50902328771221e7c7b129665111de3101484b289d23c494f82e`

The candidate, inventory, dependency graph and licenses, package file list,
pubspec lock, publish dry-run transcript, verification record, and generated
release manifest are preserved under `target/flutter-release/`. They are local
reproducible build output and are intentionally not committed.

## Local Verification

- Package metadata and every packaged version copy resolve to
  `1.2.0-beta.0`; the Flutter Swift package resolves `BotaAppleSDK` through
  SwiftPM's `app-sdk` repository identity, and local Apple dependency
  overrides are rejected.
- Generated Dart, Swift, and Kotlin Pigeon bindings match the pinned generator.
- Flutter analysis reported no issues and all 84 package tests passed.
- All 42 hosted dependency licenses were inventoried with license-file hashes.
- `flutter pub publish --dry-run` reported zero warnings.
- Fresh disposable consumers built a 52.2 MB Android release APK and a 20.6 MB
  unsigned iOS release application, then removed their temporary root.
- Candidate verification accepted the exact archive and generated manifest.
- The final Task 8 package gate regenerated this template and reproduced the
  same candidate archive and inventory.
- Publication and release workflow contracts passed 42 focused tests; the full
  release suite passed 64 tests and the tooling suite passed 86 tests.
- Rust release validation passed, including the Flutter artifact identity,
  source, normalized digest, generator, and complete sorted inventory.
- Native verification passed 96 Android unit tests and 105 Apple package tests;
  9 supervised Apple hardware tests were skipped.

## Publication Controls

The synchronized release cannot complete from a partial candidate. It consumes
the successful exact-SHA CI inventory, establishes and checksums the immutable
Apple core asset, verifies or publishes the exact native CocoaPod through the
protected environment, then requires clean public SwiftPM, CocoaPods, and Maven
resolution before rebuilding and comparing Flutter.

For the first Flutter publication, the protected job prints the exact clean-tag
interactive command but does not execute it. It then requires the public
pub.dev archive to download and verify before GitHub release completion. Future
exact version tags delegate publication to Dart's reusable pub.dev OIDC
workflow without a long-lived repository token. The legacy npm `latest` guard
remains unchanged.

## Public Evidence Unavailable

The `v1.2.0-beta.0` tag, immutable GitHub asset, synchronized Apple pod and
Maven artifacts, and pub.dev package do not yet exist. Public no-override
resolution and public-archive verification therefore remain unavailable by
design. No external publication or protected-environment action was authorized
or attempted while producing this evidence.

## Regeneration

This local candidate is bound to the pre-commit Task 8 source revision. Local
`main` contains 20 encrypted-upload-v2 commits after merge base
`03ef729e691e5d8897a52a5e6640c12fbc0f8595`; Task 8 did not merge or rebase
them. Task 9 must integrate that source and rerun the tooling to regenerate the
Apple checksum, Flutter archive and inventory, source revision, manifest, and
release evidence. These source-dependent values must not be edited by hand.
