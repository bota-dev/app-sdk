# Web Foreground Release Evidence: 1.2.0-beta.6

## Status

Supervised physical-device acceptance: **NOT RUN**. The release owner requested
public beta rollout before testing the exact published package against a
production device. This is not hardware acceptance or general availability.

Beta.6 replaces the unpublished beta.5 candidate with a per-subscription START
reply barrier and bounded test cleanup. It retains the archive-first Central
retry verification. Record the final tag, CI inventory, and public package
digest after publication. No device serial, credential, WiFi secret, recording
data, or private log belongs here.

Main CI passed, but tagged Android packaging failed in a START-dependent unit
test before the protected publish job. Beta.6 is unpublished; the tag is
immutable and must not be reused.

## Post-release acceptance

Run every applicable row in
[`docs/testing/web-physical-device.md`](../../docs/testing/web-physical-device.md)
with a supervised, exact-serial physical device and the public
`@bota.dev/web-sdk@1.2.0-beta.6` tarball if it ever becomes public. Keep all
rows `NOT RUN` until actually observed. Any failure must be triaged before
another synchronized release.
