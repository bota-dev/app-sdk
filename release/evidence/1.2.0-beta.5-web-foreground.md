# Web Foreground Release Evidence: 1.2.0-beta.5

## Status

Supervised physical-device acceptance: **NOT RUN**. The release owner requested
public beta rollout before testing the exact published package against a
production device. This is not hardware acceptance or general availability.

Beta.5 replaces the unpublished beta.4 candidate after its Android tagged test
job failed on an asynchronous START-reply test fixture. It retains the
archive-first Central retry verification. Record the final tag, CI inventory,
and public package digest after publication. No device serial, credential,
WiFi secret, recording data, or private log belongs here.

Main CI passed, but the tagged Android unit-test step stalled for over 20
minutes. The release run was cancelled before its protected publish job, so
beta.5 is unpublished. The tag is immutable and must not be reused.

## Post-release acceptance

Run every applicable row in
[`docs/testing/web-physical-device.md`](../../docs/testing/web-physical-device.md)
with a supervised, exact-serial physical device and the public
`@bota.dev/web-sdk@1.2.0-beta.5` tarball if it ever becomes public. Keep all
rows `NOT RUN` until actually observed. Any failure must be triaged before
another synchronized release.
