# Web Foreground Release Evidence: 1.2.0-beta.8

## Status

Supervised physical-device acceptance: **NOT RUN**. The release owner approved
the beta.8 rollout to correct CocoaPods packaging before production-device
testing. This does not establish Web hardware acceptance or general availability.

Beta.8 replaces the partially published beta.7. It changes Apple CocoaPods
distribution from a forbidden `prepare_command` to a checksummed archive of the
same Swift facade and XCFramework. No Web runtime behavior is intentionally
changed. Record the final tag, CI inventory, and public tarball digest after
publication. Do not include device serials, credentials, WiFi secrets,
recordings, or private logs here.

## Post-release acceptance

Run every applicable row in
[`docs/testing/web-physical-device.md`](../../docs/testing/web-physical-device.md)
with a supervised, exact-serial physical device and the public
`@bota.dev/web-sdk@1.2.0-beta.8` tarball. Keep all rows `NOT RUN` until actually
observed. A failure must be triaged before another synchronized release.
