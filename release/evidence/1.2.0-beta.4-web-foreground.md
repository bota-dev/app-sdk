# Web Foreground Release Evidence: 1.2.0-beta.4

## Status

Supervised physical-device acceptance: **NOT RUN**. The release owner requested
public beta rollout before testing the exact published package against a
production device. This is not hardware acceptance or general availability.

Beta.4 replaces the partial beta.3 release with archive-first Central retry
verification. Its automated release gates must pass for the exact tagged source.
Record the final tag, CI inventory, and public package digest after publication.
No device serial, credential, WiFi secret, recording data, or private log belongs
in this file.

The immutable beta.4 tag passed main CI and the tagged Apple, Web, React Native,
and release-verification jobs. The tagged Android packaging job failed twice in
`EncryptedUploadV2TransferControlTest` (different START-dependent cases). The
protected publish job never ran; beta.4 is unpublished. Its tag must not move.

## Post-release acceptance

Run every applicable row in
[`docs/testing/web-physical-device.md`](../../docs/testing/web-physical-device.md)
with a supervised, exact-serial physical device and the public
`@bota.dev/web-sdk@1.2.0-beta.4` tarball if it ever becomes public. Keep all
rows `NOT RUN` until actually observed. Any failure must be triaged before
another synchronized release.
