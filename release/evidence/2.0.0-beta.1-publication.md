# 2.0.0-beta.1 Publication

Date: 2026-09-24. Status: COMPLETE; all five synchronized packages are public
and fresh public consumer checks passed. Hardware acceptance: NOT RUN.
Demo and Bota One were not upgraded.

## Authority

- The release owner explicitly authorized normal CI and publication of beta.1.
- Source: `ba8974486c502e11d21d9c405b16511d5fdc0e80`.
- Immutable annotated tag: `v2.0.0-beta.1`.
- Candidate inventory SHA-256:
  `aa6298603fc44ad2492c18b708cb6c754a8c84890394d4eed6702a82134f26ee`.
- [Main CI](https://github.com/bota-dev/app-sdk/actions/runs/36046786571),
  [license](https://github.com/bota-dev/app-sdk/actions/runs/36046786294), and
  [security](https://github.com/bota-dev/app-sdk/actions/runs/36046785632)
  passed at this exact source. All 49 candidate files were independently
  downloaded and verified before tagging.
- Local checks are recorded in [preflight](2.0.0-beta.1-preflight.md).

## Protected Workflows

- [Synchronized release](https://github.com/bota-dev/app-sdk/actions/runs/36049451253): SUCCESS, attempt 6.
- [Flutter OIDC publication](https://github.com/bota-dev/app-sdk/actions/runs/36049451798): SUCCESS.
- All tagged pre-publication verification and packaging gates passed.
- The first protected publication attempt reached Central `PUBLISHING`, then
  exhausted its bounded poll window without reaching `PUBLISHED`.
- The second attempt restored and verified the original signed bundle and
  skipped signing. It also exhausted the poll window while Central still
  reported `PUBLISHING` at 20:30 UTC, without a rejected deployment.
- At 20:40 UTC, the public Maven POM matched the tagged inventory. The third
  attempt resumed the same deployment and signed inputs. Central reported
  `PUBLISHED` at 20:42 UTC, and all 30 public Maven files passed verification.
- The third attempt published React Native through OIDC with signed provenance
  ([transparency log](https://search.sigstore.dev/?logIndex=2944272412)). Its
  immediate npm tag check failed during propagation. A subsequent independent
  registry read verified the exact tagged tarball, `beta=2.0.0-beta.1`, and
  unchanged `latest=2.0.0-beta.0`. The fourth attempt verified this occupied
  version before continuing without uploading RN again.
- The fourth attempt verified RN and published Web through OIDC with signed
  provenance ([transparency log](https://search.sigstore.dev/?logIndex=2944313615)).
  Its npm tag check also preceded propagation. At 20:51 UTC, independent
  downloads matched both tagged tarballs, both `beta` tags were beta.1, both
  `latest` tags remained beta.0, and every legacy version and tag was unchanged.
  The fifth attempt resumed verification of these occupied versions.
- The fifth attempt passed native/npm publication verification, public SwiftPM
  consumption, Maven consumers on API 26 and 35, and CocoaPods Trunk publication.
  Trunk's exact beta.1 podspec pins the approved script-free CocoaPods archive
  SHA-256 `6f4df84d134845e4de2e4ee90238f26980f2bdf02d31ea1c177a0efa0d18e11b`.
  The ordered Flutter build ran only after those prerequisites passed.
- The fifth attempt's fresh CocoaPods consumer could not yet find beta.1 in
  the public CDN version index, although Trunk had registered it. Publication
  remained gated on normal CDN-backed resolution without a local podspec or
  duplicate Trunk publication.
- The CDN exposed beta.1 at 21:10 UTC. Attempt 6 passed the ordered Flutter
  candidate gate, including fresh Android/iOS release apps. All nine candidate
  files independently matched the approved main-CI inventory.
- The protected reusable pub.dev workflow published `bota_app_sdk` through
  OIDC at 21:36 UTC. No interactive registry authentication or token fallback
  was used. Both workflows verified all 57 public files and automatic release
  attachment completed, preserving the native `release-manifest.json` and
  separate `flutter-release-manifest.json`.

Central deployment: `9c322271-a74a-430f-8b5f-b3d6d96de6ea`.

Signed bundle SHA-256:
`bde8bcded8cb27818065177e7b12aedd15d4cce9e52a3de8b7656ecd03a1980f`.

Signed inventory SHA-256:
`3aae472bb29d9ee1f1352a587601d609f100de10f03a179d2fa2be20a65bff2f`.

The public [prerelease](https://github.com/bota-dev/app-sdk/releases/tag/v2.0.0-beta.1)
preserves the bundle, inventory, source inventory, deployment state, and final
publication evidence. Recovery reused the exact signed inputs; no occupied
version was replaced and the immutable tag was never moved.

## Channel Policy

The `@bota.dev/react-native-app-sdk` and `@bota.dev/web-app-sdk` `beta` tags
advanced to `2.0.0-beta.1`. Their `latest` tags remain `2.0.0-beta.0`.
All 22 old React Native and six old Web versions and their tags remain unchanged.
Pre-publication snapshots recorded old RN `latest=0.0.67`,
`beta=1.2.0-beta.11`, and old Web `latest=1.2.0-beta.0`,
`beta=1.2.0-beta.11`.

Both new-name npm OIDC uploads are proven by the tagged workflow and registry
provenance. The pub.dev OIDC publication, complete public inventory, and normal
hosted Flutter Android/iOS consumer builds also passed.

## Public Consumer Checks

Fresh local checks use registry-installed packages, not source overrides:

| Consumer | Result |
| --- | --- |
| React Native Android | Codegen, 31 adapter tests, release lint, and release AAR passed |
| React Native iOS | CocoaPods install and unsigned Release simulator application build passed |
| Web | Exact installed-file verification, Vite production build, and seven Chromium tests passed |
| Flutter Android | Hosted beta.1, release APK, and exact Maven dependency passed with both Flutter-path overrides unset |
| Flutter iOS | Hosted beta.1, exact public SwiftPM dependency, and unsigned Release device application build passed |

All 173 installed RN package files matched the approved public archive after
native builds. The Android dependency report selects
`dev.bota:bota-app-sdk:2.0.0-beta.1`; the Apple resolution pins beta.1 at
`ba8974486c502e11d21d9c405b16511d5fdc0e80`.
The RN harness redirects its fixture paths to ordinary consumer `node_modules`
and Maven Central without editing installed SDK sources. Its Android result is
an adapter AAR, not an end-user APK. Xcode resolves using its `netrc`
authorization provider to avoid this host's Keychain stall; checksums and
package identity checks remain enabled.

Public npm tarball SHA-256 values match the main-CI candidate:

- React Native: `047d4fb8f83c404f0b8c3db9c1880b26ad9da0883485d92a9a1a4e914e2789a4`.
- Web: `489a6ab43ac04cc1514fa57022f83b53fb82e9ee04dc4d0d2d32507d82238a44`.

Flutter's public archive SHA-256 is
`f0eaedcb1e134f0c7b054d2c3e7958a65eb158dbfaf0b4546754d80bc9a45608`;
its normalized archive SHA-256 is
`972c255d48ddd9518bff2fe72d2e762121bbb49a19524dad84ba4ae8d03e4566`.
The registry repacks the archive, so normalized and per-file verification
establishes candidate identity. All 57 installed files remained unchanged
after both hosted builds. The hosted lockfile pins the exact archive hash;
Maven and SwiftPM both resolve beta.1, with SwiftPM at the source above.

The first hosted resolution attempts preceded pub.dev catalog/cache
propagation. A fresh isolated pub cache subsequently resolved the ordinary
hosted dependency. No SDK source override or installed-package edit was used.
Android built with `BOTA_FLUTTER_HOME` and `FLUTTER_ROOT` unset, closing the
beta.0 normal-install gap. These are build and publication results, not
physical BLE or application-rollout acceptance.
