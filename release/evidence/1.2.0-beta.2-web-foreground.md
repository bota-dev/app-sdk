# Web Foreground Release Evidence: 1.2.0-beta.2

## Status

Supervised physical-device acceptance: **NOT RUN**. The release owner requested
public beta rollout first so the exact published package can be tested against
a production device. This is not hardware acceptance or general availability.

The tagged publish workflow failed before any registry write: its candidate
comparison omitted `schemaVersion` from the expected JSON even though every
tagged native artifact hash matched the green main-CI inventory. This version
was not published. Do not move or reuse its immutable tag; beta.3 is the
replacement candidate.

The automated package, browser, and cross-platform release gates must pass for
the exact tagged source before publication. No device serial, backend
credential, WiFi secret, recording data, or private log belongs in this file.

## Post-release acceptance

Run every applicable row in
[`docs/testing/web-physical-device.md`](../../docs/testing/web-physical-device.md)
with a supervised, exact-serial physical device and the public
`@bota.dev/web-sdk@1.2.0-beta.2` tarball. Keep all rows `NOT RUN` until actually
observed. Any failure must be triaged before another synchronized release.
